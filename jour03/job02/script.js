document.addEventListener('DOMContentLoaded', function() {
    var divs = document.querySelectorAll('.container-image-first > div');
    var divArray = Array.from(divs);
    console.log(divArray);
});
